{
  "name": "lmapi",
  "version": "1.0.0",
  "description": "- Pool multiple Ollama servers and route requests by priority and availability.\r - Queue prompt jobs with awareness of model availability per server.\r - Prompt multiple models in parallel to compare speed and quality.\r - Cache server model lists with short timeouts to detect availability quickly.\r - Persist prompt metrics (duration, tokens, temps) in SQLite for later analysis.\r - Provide logging per day with request/response tracing.",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node src/app.ts",
    "build": "tsc",
    "start": "node dist/app.js",
    "test:routes": "ts-node scripts/testRoutes.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "better-sqlite3": "^12.5.0",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "pino": "^10.1.0",
    "pino-pretty": "^13.1.3",
    "pino-roll": "^4.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.3",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
